name: Echo

on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Your message to workflow'
        required: true
        type: string

jobs:
  reply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Write response to file
        run: |
          echo "Workflow received: '${{ github.event.inputs.message }}'" > feedback.txt
          echo "And says: Hello from GitHub Actions!" >> feedback.txt
          echo "Time: $(date -u)" >> feedback.txt

      - name: Commit and push response
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add feedback.txt
          git commit -m "Update response" || echo "No changes to commit"
          git push
